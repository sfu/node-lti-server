<% if (!item.cover_url) { item.cover_url = '/libraryReserves/images/no_cover_image.png'; } %>

<div class="reserve_item">
    <a href="<%= item.item_url %>" target=_blank><img class="cover_image" src="<%= item.cover_url %>"/></a>
    <p><a href="<%= item.item_url %>" target=_blank><%= item.title %></a></p>
    <p><%= item.author %></p>
</div>